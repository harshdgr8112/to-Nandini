# Birthday Surprise

Birthday-surprise website starter structure.

## Folders

- `css/` — main, animation, and responsive styles
- `js/` — interactive site features
- `assets/images/`, `music/`, `fonts/`, and `icons/` — media assets
